{
  "name": "envilder",
  "version": "1.0.0",
  "main": "index.js",
  "description": "A CLI tool to generate .env files from AWS SSM parameters",
  "scripts": {
    "build": "tsc",
    "envilder": "ts-node src/envilder.ts",
    "test": "jest --no-cache --env=node --colors --seed=1 --coverage=true",
    "lint": "next lint",
    "format": "prettier --ignore-path .gitignore --write \"**/*.+(ts|tsx)\""
  },
  "keywords": [],
  "author": "Marçal",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.1691.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.13",
    "@types/node": "^22.5.5",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.2"
  }
}
